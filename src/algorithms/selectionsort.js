// import { swap } from './swap';
export const selectionSort = tempArr => {
	const arr = tempArr.map(item => item.val);

	for (let i = 0; i < arr.length; i++) {
		// let minIdx = i;
		for (let j = i + 1; j < arr.length; i++) {
			// code
		}
	}
};
